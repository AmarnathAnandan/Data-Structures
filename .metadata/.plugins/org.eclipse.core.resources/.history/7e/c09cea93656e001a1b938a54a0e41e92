package executablee;

import java.util.Arrays;
import java.util.Scanner;

public class BigD {
	   public static void main(String []args){
	        //Input
	        Scanner sc= new Scanner(System.in);
	        int n=sc.nextInt();
	        String []s=new String[n+2];
	        for(int i=0;i<n;i++){
	            s[i]=sc.next();
	        }
	      	sc.close();

	        double[] ia = Arrays.stream(s).mapToDouble(Double::parseDouble).toArray();
	      	
	      //	Arrays.sort((Integer)s);
	      //	Arrays.

	        //Output
	        for(int i=0;i<n;i++)
	        {
	            System.out.println(ia[i]);
	        }
	    }

	
}
